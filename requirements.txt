faster-whisper>=1.0.0
customtkinter>=5.2.0
Pillow>=10.0.0
requests>=2.31.0
websockets>=12.0
yt-dlp>=2024.1.1

# Editor preview audio playback (ui/preview_player.py). Bundles PortAudio in
# its wheel — no system install needed. Imported lazily/optionally: the
# Editor's preview still works (video-only) if this is ever missing.
sounddevice>=0.4.6

# Vendored research engine (research/engine/) — lightweight core only,
# no Crawl4AI/Playwright extra. See research/engine/README.md.
pydantic>=2.0
httpx>=0.27
beautifulsoup4>=4.12
lxml>=5.0
imagesize>=1.4
chompjs>=1.3
